export const SLOT_SIZE = 64; // Slot size in pixels
export const SLOT_MARGIN = 4;
export const TOTAL_SLOT_SIZE = SLOT_SIZE + (SLOT_MARGIN * 2);

export const NUM_ROWS = 8;
export const NUM_COLS = 12;